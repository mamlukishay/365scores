{
  "_comment": "Local-dev only. Copy to secrets/tokens.json (gitignored) and fill in real bearer tokens. In CI, the same JSON is provided via the USER_TOKENS GitHub Actions secret. Map is { slug: token }.",
  "example": "eyJ... paste a 365scores bearer token here ..."
}
